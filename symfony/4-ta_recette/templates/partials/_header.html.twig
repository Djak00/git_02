<nav class="navbar navbar-expand-lg bg-primary navbar-dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('home') }}">TaRecette 🍽️</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{ path('home') }}">Accueil 🏠</a>
				</li>
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('ingredient') }}">Ingrédients 🥗</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('recette') }}">Recettes 📋</a>
					</li>
				{% endif %}
			</ul>
			<div class="d-flex">
				<ul class="navbar-nav me-auto">
					{% if app.user %}
						<li class="nav-item dropdown" style='margin-right: 7rem;'>
							<a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
								{{ app.user.fullName }}
							</a>
							<ul class="dropdown-menu" aria-labelledby="userDropdown">
								<li>
									<a class="dropdown-item" href="{{ path('modification', {id: app.user.id}) }}">Modifier mes informations</a>
								</li>
								<li>
									<a class="dropdown-item" href="{{ path('utilisateur.modification.password', {id: app.user.id}) }}">Modifier mon mot de passe</a>
								</li>
								<li><hr class="dropdown-divider"></li>
								<li>
									<a class="dropdown-item" href="{{ path('deconnexion') }}">Déconnexion</a>
								</li>
							</ul>

						</li>
					{% else %}
						<li class="nav-item">
							<a class="nav-link" href="{{ path('connexion') }}">Connexion 🔑
							</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{ path('inscription') }}">Inscription 📋</a>
						</li>
					{% endif %}
				</ul>
			</div>

		</div>
	</div>
</nav>
