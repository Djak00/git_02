<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		<link rel="stylesheet" href="https://bootswatch.com/5/flatly/bootstrap.min.css">
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
		<link rel="stylesheet" href="{{ asset('css/main.css') }}"> {% block stylesheets %}{% endblock %}
		</head>
		<body>

			<!-- Navigation -->
			<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
				<div
					class="container-fluid">
					{# Utilisation de container-fluid pour étendre sur toute la largeur #}
					<a class="navbar-brand" href="{{ path('affich_aliments') }}">FaitTaRecette.com</a>
					<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
						<span class="navbar-toggler-icon"></span>
					</button>

					<div class="collapse navbar-collapse" id="navbarColor01">
						<ul
							class="navbar-nav me-auto">
							<!-- Menu Aliments -->
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle" href="#" id="navbarDropdownAliments" role="button" data-bs-toggle="dropdown" aria-expanded="false">
									Aliments
								</a>
								<ul class="dropdown-menu" aria-labelledby="navbarDropdownAliments">
									<li>
										<a class="dropdown-item" href="{{ path('affich_aliments') }}">Voir les Aliments</a>
									</li>
									<li><hr class="dropdown-divider"></li>
									{% if is_granted('ROLE_ADMIN') %}
									<li>
										<a class="dropdown-item" href="{{ path('affich_admin') }}">Gérer les Aliments</a>
									</li>
									{% endif %}
								</ul>
							</li>

							<!-- Menu Catégories -->
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle" href="#" id="navbarDropdownTypes" role="button" data-bs-toggle="dropdown" aria-expanded="false">
									Catégories
								</a>
								<ul class="dropdown-menu" aria-labelledby="navbarDropdownTypes">
									<li>
										<a class="dropdown-item" href="{{ path('affich_categories') }}">Voir les Catégories</a>
									</li>
									<li><hr class="dropdown-divider"></li>
									{% if is_granted('ROLE_ADMIN') %}
									<li>
										<a class="dropdown-item" href="{{ path('affich_adminCategories') }}">Gérer les Catégories</a>
									</li>
									{% endif %}
								</ul>
							</li>
							{% if not app.user %}
								<li class="nav-item dropdown">
									<a class="nav-link " href="{{path('inscription')}}">
										Inscription
									</a>
								</li>
								<!-- Menu Connexion -->
								<li class="nav-item dropdown">
									<a class="nav-link " href="{{path('connexion')}}">
										Connexion
									</a>
								</li>
								{% else %}
								<li class="nav-item dropdown">
									<a class="nav-link " href="{{path('deconnexion')}}">
										Déconnexion
									</a>
								</li>
							{% endif %}
						</ul>

						<!-- Barre de recherche -->
						<form class="d-flex">
							<input class="form-control me-sm-2" type="search" placeholder="Rechercher">
							<button class="btn btn-secondary my-2 my-sm-0" type="submit">🔎</button>
						</form>
					</div>
				</div>
			</nav>

			<!-- Titre principal -->
			<h1 class="rounded border border-dark bg-warning p-2 m-2 text-white"> {% block monTitre %}{% endblock %}
				</h1>

				<!-- Contenu principal -->
				<main class="container mt-4"> {% block body %}{% endblock %}
					</main>

					<!-- Scripts Bootstrap (dropdowns, collapses) -->
					<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>

				</body>
			</html>

