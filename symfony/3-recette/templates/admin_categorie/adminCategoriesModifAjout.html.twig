{% extends 'base.html.twig' %}

{% block title %}Modification {{categorie.libelle}}
{% endblock %}
{% block monTitre %}
    {{ categorie.id ? 'Modification : ' ~ categorie.libelle : 'Ajout d\'une nouvelle catégorie' }}
{% endblock %}



{% block body %}

{{form_start(form)}}
{{ form_errors(form) }}
{{ form_widget(form._token) }}
{% if categorie.id %}
<img src="{{asset('images/categories/' ~ categorie.image)}}" alt="img" style="width : 200px" >
{% endif %}


{{form_row(form.imageFile)}}
    {{form_widget(form)}}
    <input class='btn btn-info' type="submit" value='Valider'>
{{form_end(form)}}
{% endblock %}

